import { DigitLimitDirective } from './digit-limit.directive';

describe('DigitLimitDirective', () => {
  it('should create an instance', () => {
    const directive = new DigitLimitDirective();
    expect(directive).toBeTruthy();
  });
});
