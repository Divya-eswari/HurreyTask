import { ShowDigitsDirective } from './show-digits.directive';

describe('ShowDigitsDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowDigitsDirective();
    expect(directive).toBeTruthy();
  });
});
