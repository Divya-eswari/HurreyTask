import { RestrictNameDirective } from './restrict-name.directive';

describe('RestrictNameDirective', () => {
  it('should create an instance', () => {
    const directive = new RestrictNameDirective();
    expect(directive).toBeTruthy();
  });
});
